# إعداد الإرسال التلقائي إلى Telegram

هذه النسخة تنسخ موقع RA3D BET إلى مستودع مستقل، وتضيف طابورًا للتوقعات وخدمة خلفية تعمل عبر GitHub Actions. الموقع يضيف التوقع المؤهل إلى `telegramQueue`، والعامل الخلفي يرسله إلى نفس بوت التقارير ثم يسجل الحالة في `telegramSent` ويحدّث السجل إلى `sent`.

## Secrets المطلوبة

أضف في إعدادات المستودع `raedemed/RA3D-BET-VIP`:

- `FIREBASE_SERVICE_ACCOUNT_JSON`: بيانات حساب خدمة Firebase بصيغة JSON، بصلاحية الوصول إلى Firestore.
- `TELEGRAM_BOT_TOKEN`: نفس Bot Token المستخدم لبوت التقارير.
- `TELEGRAM_CHAT_ID`: نفس Chat ID المستخدم لبوت التقارير.

يستخدم العامل القيم الموجودة في مستند Firebase الخاص `private/telegram` أولًا، ثم يرجع إلى Secrets كبديل. لا تضع هذه القيم في ملفات الموقع أو داخل Git.

## التشغيل

يعمل الـ workflow كل خمس دقائق، ويمكن تشغيله يدويًا من تبويب Actions. قبل الإرسال يفحص إعدادات `telegramAuto` من `config/settings`. إذا كان `enabled` غير مفعّل فلن يرسل شيئًا.

## منع التكرار

كل توقع يحصل على معرّف ثابت مبني من معرّف المباراة والتاريخ. يتم حجزه داخل Firestore قبل الإرسال في معاملة، ثم يسجل `telegramMessageId` بعد نجاح Telegram. لذلك لا يعاد إرسال التوقع في التشغيلات العادية أو عند إعادة تشغيل العامل.

## ملاحظة تشغيلية

GitHub Actions يحتاج تفعيلًا وصلاحية الأسرار في المستودع. من لوحة الأدمن فعّل **إرسال التوقعات تلقائيًا**، وحدد الحد الأدنى للثقة والحد اليومي، ثم احفظ إعدادات الإرسال.
